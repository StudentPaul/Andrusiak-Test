import { ImageMinHeightDirective } from './image-min-height.directive';

describe('ImageMinHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageMinHeightDirective();
    expect(directive).toBeTruthy();
  });
});
