import { ImageMaxSizeDirective } from './image-max-size.directive';

describe('ImageMaxSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageMaxSizeDirective();
    expect(directive).toBeTruthy();
  });
});
