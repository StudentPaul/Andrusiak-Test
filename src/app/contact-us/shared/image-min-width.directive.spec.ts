import { ImageMinWidthDirective } from './image-min-width.directive';

describe('ImageMinWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageMinWidthDirective();
    expect(directive).toBeTruthy();
  });
});
